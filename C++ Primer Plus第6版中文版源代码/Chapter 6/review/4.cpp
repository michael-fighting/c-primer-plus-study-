weight >= 115 && weight < 125;
ch = 1 || ch = Q;
x % 2 == 0 && x != 26;
x % 2 -- 0 && x / 26 != 0;
(donation >= 1000 && donation <= 2000) || guest == 1;
(ch >= 'a' && ch <= 'z') || (ch >= 'A' && ch <= 'Z') 

 if (x < 0)
 {
    x = -x;
 }
 
 char x;
 int a_grade;
 switch (x)
 {
 case 'A':
    /* code */
    a_grade++;
    break;
case 'B':
    b_grade++;
    break;
 
 default:
    break;
 }